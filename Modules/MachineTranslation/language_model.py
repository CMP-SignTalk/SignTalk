def language_model(target_corpus):
    """
    A function that calculates the probability of a target sentence, given no context from the source sentence.

    Args:
    - target_corpus (list of str): A list of sentences in the target language.

    Returns:
    - language_model_probabilities (dict): A dictionary mapping each target sentence to its probability.
    """
    # TODO: implement a language model for calculating P(e)
    language_model_probabilities = {}
    return language_model_probabilities
