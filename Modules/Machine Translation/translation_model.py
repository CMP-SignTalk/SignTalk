def translation_model(source_corpus, target_corpus):
    """
    A function that calculates the probability of a translation pair (f,a), given a target sentence e.

    Args:
    - source_corpus (list of str): A list of sentences in the source language.
    - target_corpus (list of str): A list of sentences in the target language.

    Returns:
    - translation_model_probabilities (dict): A dictionary mapping each source sentence to a dictionary of translation probabilities for each possible target sentence.
    """
    # TODO: implement a translation model for calculating P(f,a|e)
    translation_model_probabilities = {}
    return translation_model_probabilities
