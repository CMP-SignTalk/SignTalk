def load_corpus(path):
    """
    A function that loads a corpus from a file.

    Args:
    - path (str): The path to the file containing the corpus.

    Returns:
    - corpus (list of str): A list of sentences in the corpus.
    """
    corpus = []
    # with open(path, "r") as f:
    #     for line in f:
    #         corpus.append(line.strip())
    return corpus